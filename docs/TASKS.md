# Tasks

* `oinstance` to find instances in memory via heap scanning
* `ocat`
* Add categories to `ocls`?
* `ocall myfile.m` to run actual objc?

## Protos

* `oprotos` -> `oproto`... maybe start again?
* `oprotos` mess